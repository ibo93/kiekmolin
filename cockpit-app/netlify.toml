# Kurani Command Center — Netlify-Konfiguration (eigene Site, getrennt von kiekmolin.de)
[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  # /.netlify/functions/* wird automatisch vor diesem Catch-all bedient.
