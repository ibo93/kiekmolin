# Secrets - niemals committen
.env

# Deine persoenliche Ordner-Zuordnung (Pfade auf DEINEM Rechner).
# Nur die Vorlage ordner.json.example ist eingecheckt.
ordner.json

# Mitschrift der Gespraeche: kann Kundendaten, Preise und Notizen
# enthalten - bleibt auf dem Rechner.
protokoll/

node_modules/
package-lock.json
